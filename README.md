# Readme

## Project Setup

```bash
mkdir <proj-name>
npm init -y
npm install typescript ts-node-dev @types/node @types/express --save-dev
npm install express
npx tsc --init
git init
```

- add simpe express rest endpoint
- run `npm start`
